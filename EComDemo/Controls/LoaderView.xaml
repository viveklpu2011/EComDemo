<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EComDemo.Controls.LoaderView">
    <ContentView.Content>
        <StackLayout BackgroundColor="#A0000000">
            <StackLayout VerticalOptions="CenterAndExpand"
                               HorizontalOptions="CenterAndExpand">
                 <ActivityIndicator Color="Yellow" IsEnabled="True" IsRunning="True"
                               VerticalOptions="CenterAndExpand"
                               HorizontalOptions="CenterAndExpand"/>
            <Label Text="Loading...."
                   VerticalOptions="CenterAndExpand"
                               HorizontalOptions="CenterAndExpand"
                   TextColor="White"
                   FontAttributes="Bold"/>
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</ContentView>
